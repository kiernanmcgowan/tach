{
  "name": "tach",
  "description": "Utility module to detect if a thread is blocked",
  "version": "0.1.0",
  "scripts": {
    "test": "node_modules/.bin/jasmine-node --captureExceptions test/"
  },
  "dependencies": {
    "underscore": "~1.6.0"
  },
  "devDependencies": {
    "jasmine-node": "~1.14.5",
    "sleep": "~1.1.8"
  }
}
